{
  "project": "Hangeul-mcp",
  "summary": "Korean HWP/HWPX form-fill MCP server. Client-agnostic (Claude Desktop, Codex, Antigravity 2.0). Python. v1 headless fill, v2 COM live-apply via pyhwpx put_field_text. Differentiators: inline-blank fill, merged-cell 2D understanding, review-then-apply.",
  "status": "v0.1.0 released; codex QA passed (all findings fixed)",
  "planApprovedByUser": true,
  "note": "This run is plan-first. Build loop starts only after user approves PLAN.md and answers the open questions in section 10. All stories passes:false. | Stabilization pass (ralplan consensus, .omo/plans/hangeul-mcp-stabilization-ralplan.md): stories US-047..US-060 ARE recorded in this file (BC3); pass count == passes==true.",
  "stories": [
    {
      "id": "US-000",
      "title": "Plan & scope approval gate",
      "milestone": "M0",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "PLAN.md exists with architecture, MCP tool API, data model, roadmap, test strategy",
        "prd.json exists with concrete task-specific acceptance criteria per story",
        "User has reviewed PLAN.md and answered the 5 open questions (section 10)",
        "Repo location, python-hwpx-vs-own-engine decision, and matching-key decision are confirmed"
      ],
      "status": "complete"
    },
    {
      "id": "US-001",
      "title": "Repo scaffold + packaging",
      "milestone": "M0",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "Directory layout matches PLAN.md section 4 (hangeul_core, hangeul_mcp, tests, docs)",
        "pyproject.toml defines package + console entrypoint 'hangeul-mcp'",
        "LICENSE (MIT), README.md, .gitignore present",
        "git repo initialized; `pip install -e .` (or uv sync) succeeds with no error",
        "Sample fixtures copied into tests/fixtures (blank + filled 강사카드)"
      ],
      "status": "complete"
    },
    {
      "id": "US-002",
      "title": "OWPML container I/O (byte-preserving) + substrate decision",
      "milestone": "M1",
      "priority": 3,
      "passes": true,
      "acceptanceCriteria": [
        "Spike evaluates python-hwpx as substrate; decision recorded in docs/architecture.md (adopt or use own engine)",
        "unpack/repack preserves mimetype as first entry with STORED compression",
        "XML declaration (standalone=\"yes\") preserved byte-for-byte after repack",
        "Round-trip test: repack of untouched file is byte-identical to original (or documented minimal diff)",
        "test_owpml passes"
      ],
      "status": "complete"
    },
    {
      "id": "US-003",
      "title": "Form analyzer (tables, cells, spans, paraPr, charPr)",
      "milestone": "M1",
      "priority": 4,
      "passes": true,
      "acceptanceCriteria": [
        "analyze() returns tables with cells addressed by rowAddr/colAddr and colSpan/rowSpan",
        "Detects empty cells, bullet paragraphs (heading type=BULLET), and charPr spacing",
        "Golden test on 강사카드(양식).hwpx reproduces the known 표2 field set (성명, 주민등록번호, 학력, 경력×3, 현직, etc.)",
        "test_analyze passes"
      ],
      "status": "complete"
    },
    {
      "id": "US-004",
      "title": "Form understanding: 2D label-value mapping (merged-cell aware)",
      "milestone": "M1",
      "priority": 5,
      "passes": true,
      "acceptanceCriteria": [
        "Maps labels to their value cells including label-above-value and merged (span) layouts",
        "Emits FieldSchema per PLAN.md section 5 with field kind classification",
        "On the 강사카드(양식) fixture, correctly identifies 성명/직위/휴대전화 value cells (not adjacent labels)",
        "test_understand passes"
      ],
      "status": "complete"
    },
    {
      "id": "US-005",
      "title": "Inline-blank detector (mid-sentence / mid-cell blanks)",
      "milestone": "M1",
      "priority": 6,
      "passes": true,
      "acceptanceCriteria": [
        "Detects patterns like '∘ ___ 을 졸업하시고' and '은행명: ___ 계좌번호: ___' as kind=inline_blank with insert positions",
        "Distinguishes inline_blank from empty_cell and bullet_item",
        "Golden test on the 강사카드(양식) narrative rows (학력/경력/현직) passes",
        "test_inline passes"
      ],
      "status": "complete"
    },
    {
      "id": "US-006",
      "title": "Format-preserving fill engine (set/append/inline)",
      "milestone": "M1",
      "priority": 7,
      "passes": true,
      "acceptanceCriteria": [
        "Fills empty_cell (set), bullet_item (append after marker, no duplicate '-'/'∘'), inline_blank (mid-run insert)",
        "Multi-line values become real paragraphs/lineBreaks (no raw \\n inside <hp:t>)",
        "Spacing normalization option produces spacing=0 without affecting untouched runs",
        "Round-trip test: filled fields present AND all untouched regions byte-identical AND well-formed AND mimetype/declaration preserved",
        "test_fill passes"
      ],
      "status": "complete"
    },
    {
      "id": "US-007",
      "title": "MCP server (v1 headless) via FastMCP stdio",
      "milestone": "M2",
      "priority": 8,
      "passes": true,
      "acceptanceCriteria": [
        "Server built on official MCP Python SDK (FastMCP), stdio transport",
        "Exposes tools: detect_format, analyze_form, fill_form, extract_text with JSON schemas",
        "An MCP client smoke test (or mcp dev/inspector) lists the 4 tools and calls fill_form end-to-end on a fixture",
        "Tools are pure (params in -> JSON out), no client-specific assumptions"
      ],
      "status": "complete"
    },
    {
      "id": "US-008",
      "title": "Client-agnostic config docs + verification",
      "milestone": "M2",
      "priority": 9,
      "passes": true,
      "acceptanceCriteria": [
        "docs/clients/ contains registration snippets for Claude Desktop, Codex, and Antigravity 2.0",
        "Server starts via the documented stdio command for each client",
        "README documents that only MCP tools are used (max client compatibility)",
        "At least one non-Claude client (Codex or Antigravity) verified to list tools"
      ],
      "status": "complete"
    },
    {
      "id": "US-009",
      "title": "COM bridge (v2) via pyhwpx with environment guard",
      "milestone": "M3",
      "priority": 10,
      "passes": true,
      "acceptanceCriteria": [
        "hwp/com.py attaches to a running 한글 instance, get_field_list works, put_field_text(dict) fills fields",
        "Off-Windows or 한글-not-installed: import/guard returns available:false gracefully (no crash)",
        "Security file-access prompt handling documented (FilePathCheckerModule)",
        "Guarded integration test passes on a machine with 한글 (skips elsewhere)"
      ],
      "status": "desktop-live-pending",
      "statusNote": "COM bridge code + headless-gated tests complete; real-desktop COM evidence pending (US-053 / PENDING_DESKTOP_LIVE_QA)."
    },
    {
      "id": "US-010",
      "title": "apply_to_open_hwp MCP tool + review->apply workflow",
      "milestone": "M3",
      "priority": 11,
      "passes": true,
      "acceptanceCriteria": [
        "hwp_status and apply_to_open_hwp tools exposed",
        "apply runs format-preserving preprocessing on values before put_field_text",
        "Returns applied[] and skipped[] with reasons; returns needs_field_registration when no named fields exist",
        "End-to-end demo: analyze -> (LLM draft) -> apply to open 한글 fills the live document one-shot"
      ],
      "status": "desktop-live-pending",
      "statusNote": "apply_to_open_hwp code complete; real-desktop put_field_text evidence pending (US-053)."
    },
    {
      "id": "US-011",
      "title": "Review->apply Agent Skill (thin orchestration)",
      "milestone": "M4",
      "priority": 12,
      "passes": true,
      "acceptanceCriteria": [
        "skills/SKILL.md guides: analyze_form -> draft -> user review -> fill_form or apply_to_open_hwp",
        "Skill instructs value generation to respect field kind (bullets, inline, checkbox)",
        "Documented usage from Claude Desktop"
      ],
      "status": "complete"
    },
    {
      "id": "US-012",
      "title": "CI, golden fixtures, .hwp policy, release",
      "milestone": "M4",
      "priority": 13,
      "passes": true,
      "acceptanceCriteria": [
        "CI runs tests on push (GitHub Actions) green",
        ".hwp input policy implemented per user decision (reject-with-guidance or auto-convert)",
        "README quickstart verified; version tag cut",
        "Coverage of core modules documented"
      ],
      "status": "complete"
    },
    {
      "id": "US-013",
      "title": "{placeholder} global replace + shared section-wide locate/splice primitive",
      "milestone": "M5-PhaseA",
      "priority": 14,
      "passes": true,
      "notes": "Build order FIRST: establishes the shared per-paragraph concatenated-text->char-span mapper reused by US-014/US-015/US-016. Own core (gap-analysis 3-A), not delegation.",
      "acceptanceCriteria": [
        "New KIND_PLACEHOLDER in schema; detect_placeholders(path) scans every Contents/section*.xml for {token} occurrences (itertext-aware) and emits Field(kind=placeholder, label=token-name-without-braces)",
        "Detects tokens even when split across runs/<hp:t> within a paragraph (shared paragraph offset mapper) and tokens inside table/nested-table cells",
        "fill_form replaces EVERY occurrence of each provided {token} with the client value (braces removed), preserving the token's run/charPr; duplicate tokens all replaced",
        "Values are client-provided only (brain/hand separation): unknown tokens are left byte-untouched and reported in skipped/unfilled",
        "Byte-preservation: every unchanged ZIP entry payload byte-identical (a.read(name)==b.read(name)); mimetype first STORED; section keeps standalone=\"yes\" declaration; section well-formed (ET.fromstring)",
        "Golden test (synthetic hwpx) + regressions: token split across two runs, token in nested table, same token twice; pyflakes clean; all prior 56 tests still green"
      ],
      "status": "complete"
    },
    {
      "id": "US-014",
      "title": "형광펜(markpen) placeholder kind",
      "milestone": "M5-PhaseA",
      "priority": 15,
      "passes": true,
      "acceptanceCriteria": [
        "New KIND_MARKPEN; detector records each hp:markpenBegin..hp:markpenEnd highlighted text (carried as tail) plus location, deriving label from nearest preceding label with a synthesized fallback",
        "analyze_form returns a markpen field per highlighted region with the sample text; multiple regions become distinct fields",
        "fill_form replaces ONLY the highlighted text, leaving hp:markpenBegin/hp:markpenEnd tags intact so the highlight is preserved (assert both new text and markpen tags present)",
        "Existing tests/test_markpen.py (analyze reads highlighted itertext) stays green — the itertext reading is not broken",
        "Byte-preservation + well-formed + mimetype/declaration golden pattern; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-015",
      "title": "체크박스(☑/□) 탐지·선택 (KIND_CHECKBOX)",
      "milestone": "M5-PhaseA",
      "priority": 16,
      "passes": true,
      "acceptanceCriteria": [
        "Add options field to Field dataclass and include it in hangeul_mcp.server._field_dict (additive, backward-compatible)",
        "detect_checkbox groups consecutive [glyph][label] tokens (glyph set □ ☐ ☑ ☒ ■ ▣ √) within a paragraph/cell into one KIND_CHECKBOX field with options=[{label, checked}]",
        "analyze_form JSON includes options with per-option checked state",
        "fill_form value = option label(s) to check: toggles chosen option box □->☑; default exclusive unchecks the others (☑->□); checkbox_exclusive=False leaves others unchanged; unknown option -> skipped, no glyph changed",
        "Byte-preservation + well-formed + mimetype/declaration; regressions: glyph split across runs, re-checking an already-checked option is idempotent; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-016",
      "title": "누름틀(form field) 헤드리스 fill (no COM)",
      "milestone": "M5-PhaseA",
      "priority": 17,
      "passes": true,
      "notes": "Spike the real hp:fieldBegin..hp:fieldEnd XML shape first; detector regex is attribute-order robust.",
      "acceptanceCriteria": [
        "New KIND_FORMFIELD; detect_form_fields scans hp:fieldBegin(name=..)..hp:fieldEnd controls, resolving name from the name attribute or a parameterSet, and records the field's text span",
        "analyze_form returns form_field fields keyed by field name/label",
        "fill_form fills the field content headlessly (no COM), preserving hp:fieldBegin/hp:fieldEnd controls (replace existing placeholder text, not append)",
        "Value schema parity with COM apply_to_open_hwp: the same name-keyed values dict works for both headless and COM paths (documented; unit test on key handling)",
        "No named fields -> analyze returns none of this kind gracefully (consistent with COM needs_field_registration)",
        "Byte-preservation + well-formed + mimetype/declaration; regressions: parameterSet-named vs attribute-named field; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-017",
      "title": "form-fit / 쪽수 드리프트 가드 (overflow guard + opt-in auto-fit)",
      "milestone": "M5-PhaseA",
      "priority": 18,
      "passes": true,
      "notes": "No renderer available; overflow is a documented heuristic estimate, not layout truth. True visual verification deferred to Phase B render_preview.",
      "acceptanceCriteria": [
        "Tier1 (MUST): analyze_formfit(path, values) estimates per-field fill width (char classes x font height x width factor) vs cell capacity (cellSz/hp:sz width in HWPUNIT minus inMargin) and returns overflow warnings with field_id + estimated ratio; a comfortably-fitting value yields no warning; pure measurement, no mutation",
        "Heuristic (formula + Hangul/latin width factors + assumptions) is documented and unit-tested on synthetic widths; explicit non-goal: pixel-perfect layout",
        "Tier2 (opt-in): fill_form(auto_fit=True) shrinks only the target run's font/spacing via a cloned charPr (reusing the _clone_charpr pattern) stepwise to a documented min floor; reports applied shrink per field; untouched runs stay byte-identical; no zero/negative sizes",
        "auto_fit=False (default) never mutates formatting — pure fill, backward-compatible with all existing fill tests",
        "Byte-preservation + well-formed + mimetype/declaration; golden/regression: overflow fixture flagged, fitting value no-op, auto-fit floor respected; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-018",
      "title": "PII 마스킹 게이트 (코드화)",
      "milestone": "M6-PhaseB",
      "priority": 19,
      "passes": true,
      "acceptanceCriteria": [
        "hangeul_core/pii.py: scan_text(text) detects 주민등록번호/외국인등록번호, 전화번호, 신용카드번호, 계좌번호(heuristic), 이메일 with type + span + masked form",
        "mask_value(text) masks detected PII (keeps format, hides sensitive digits) without altering non-PII text",
        "fill_form(mask_pii=True) masks PII inside provided values before writing and reports a masked[] list; mask_pii=False (default) is unchanged behavior",
        "New MCP tool scan_pii(path) audits a document's text and returns findings (no mutation)",
        "Tests use synthetic (fake) PII patterns only; no real PII committed; golden + regression; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-019",
      "title": "dry-run + 편집 전 백업",
      "milestone": "M6-PhaseB",
      "priority": 20,
      "passes": true,
      "acceptanceCriteria": [
        "fill(dry_run=True) computes filled/skipped/shrunk WITHOUT writing out_path (no file created); returns the same report shape",
        "fill(backup=True) copies the destination to <out>.bak before overwriting an existing file",
        "fill_form exposes dry_run and backup; defaults preserve current behavior",
        "Golden + regression: dry-run writes nothing, backup created only when overwriting; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-020",
      "title": "읽기 확장 (find_text / outline / styles)",
      "milestone": "M6-PhaseB",
      "priority": 21,
      "passes": true,
      "acceptanceCriteria": [
        "find_text(path, query) returns occurrences with section + containing cell/paragraph context and a total count (read-only)",
        "get_document_outline(path) returns a structural summary: sections, tables (rows/cols), field/kind counts, and heading-like paragraphs",
        "list_styles(path) returns charPr/paraPr style ids with key attributes (font height, spacing, bullet)",
        "Three new read-only MCP tools; built on existing analyze/extract; no mutation; golden tests; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-021",
      "title": "validate_hwpx (well-formed + optional XSD)",
      "milestone": "M6-PhaseB",
      "priority": 22,
      "passes": true,
      "acceptanceCriteria": [
        "validate_hwpx(path) checks: every XML entry well-formed (ET.fromstring), mimetype first + STORED + correct value, section XML declaration present",
        "Returns {valid, well_formed, mimetype_ok, declaration_ok, errors[], xsd:{available, valid?}}; a corrupted XML entry yields valid=False with a specific error",
        "If python-hwpx is installed, run its XSD validation and include the result; otherwise xsd.available=False (soft dependency, D1)",
        "New MCP tool validate_hwpx; golden + regression (valid passes, corrupted fails); pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-022",
      "title": "일반 텍스트 치환 (search_and_replace / batch_replace) — OWN, byte-preserving",
      "milestone": "M7-PhaseC",
      "priority": 23,
      "passes": true,
      "notes": "Done with our own byte-splice engine (locate.replace_literals), NOT python-hwpx — preserves the byte invariant and needs no new dependency. Structural editing (paragraphs/tables/rich formatting/images) remains DELEGATE to python-hwpx pending a dependency decision.",
      "acceptanceCriteria": [
        "locate.replace_literals(section, mapping) replaces arbitrary literal substrings inside <hp:t> text (run-split aware, boundary-guarded so a match never bridges a cell/paragraph/table), longest-match-first with no chained re-replacement",
        "search_and_replace(path, find, replace, out_path) replaces all occurrences and returns a count; batch_replace(path, mapping, out_path) applies multiple find->replace pairs in a single pass",
        "Byte-preservation: unchanged ZIP entries byte-identical; mimetype first STORED; section keeps standalone declaration; well-formed",
        "Two new MCP tools (search_and_replace, batch_replace); tags/attributes never touched (only <hp:t> text)",
        "Golden + regression (run-split match, multiple pairs, overlapping finds longest wins, no cross-cell bleed); pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-023",
      "title": "DELEGATE 내보내기/미리보기 (python-hwpx) — hwpx_to_html / hwpx_to_markdown",
      "milestone": "M8-PhaseC-D",
      "priority": 24,
      "passes": true,
      "notes": "python-hwpx is a soft/optional dependency (extra 'delegate'). Its warnings go to stderr, safe for the stdio MCP server. Read-only, so no byte concern. Partially delivers Phase B render_preview (HTML) too.",
      "acceptanceCriteria": [
        "hangeul_core/delegate.py guards python-hwpx as a soft dependency: available() check + a clear RuntimeError/'available:false' when it is not installed (core stays dependency-light)",
        "to_html / to_markdown / to_text_rich wrap HwpxDocument.export_html/export_markdown/export_text",
        "New read-only MCP tools hwpx_to_html and hwpx_to_markdown return the rendered string (or available:false)",
        "pyproject declares an optional extra 'delegate' = python-hwpx; tests use pytest.importorskip('hwpx') so they skip cleanly where the dep is absent (CI stays green)",
        "Golden tests on the real fixture assert the rendered output contains known text (성명); pyflakes clean; full suite green"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-024",
      "title": "DELEGATE 구조 편집 (python-hwpx) — add_paragraph / add_table + 검증 게이트",
      "milestone": "M8-PhaseC-D",
      "priority": 25,
      "passes": true,
      "notes": "Delegated structural edits re-serialize via lxml (NOT byte-identical) — the invariant here is our validate_hwpx gate, not byte preservation. Our own fill/recognition core keeps byte-splice.",
      "acceptanceCriteria": [
        "add_paragraph(path, text, out_path) and add_table(path, rows, cols, out_path) wrap python-hwpx, saving via save_to_path (not the deprecated save)",
        "Each delegated edit runs the output through validate_hwpx and includes the report; the edit is only reported success when valid is True",
        "New MCP tools add_paragraph and add_table; available:false when python-hwpx is absent",
        "Tests (importorskip hwpx): new content present in the output AND validate_hwpx valid is True; pyflakes clean; full suite green"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-025",
      "title": "DELEGATE 문서 생성 (python-hwpx) — markdown/text → HWPX",
      "milestone": "M9-PhaseD",
      "priority": 26,
      "passes": true,
      "notes": "두뇌·손 분리 유지: 문안 생성은 클라이언트 LLM, 서버는 구조 조립만.",
      "acceptanceCriteria": [
        "create_from_markdown(markdown, out_path) builds a new HWPX from client-provided markdown/text via python-hwpx (converter or add_paragraph assembly)",
        "New MCP tool create_hwpx_from_markdown; available:false when python-hwpx is absent",
        "Output passes validate_hwpx (valid True) and contains the provided content",
        "Tests (importorskip hwpx): created file valid + content present; pyflakes clean; full suite green"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-026",
      "title": "mail_merge (대량 생성) — OWN, 우리 fill 엔진 + 레코드 반복",
      "milestone": "M9-PhaseD",
      "priority": 27,
      "passes": true,
      "notes": "OWN (byte-preserving fill per record), not python-hwpx. Reuses every fill kind (empty_cell/inline/placeholder/markpen/checkbox/누름틀). 두뇌·손 분리: records are client-provided.",
      "acceptanceCriteria": [
        "hangeul_core/mailmerge.py: mail_merge(template_path, records, out_dir) fills the template once per record via the byte-preserving fill engine and writes numbered outputs",
        "Returns a per-record summary (index, out_path, filled/skipped counts) and a total count; supports the same value keys as fill (field_id/label/placeholder/checkbox/markpen/누름틀)",
        "Each output is a byte-preserving fill of the template (untouched entries identical) and passes validate_hwpx (valid True)",
        "New MCP tool mail_merge(template_path, records, out_dir, mask_pii=false); records are client-provided",
        "Golden test: N records -> N files, each containing its own values; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-027",
      "title": "DELEGATE 이미지 삽입 (add_image / 도장·서명) — python-hwpx",
      "milestone": "M8-PhaseC-D",
      "priority": 28,
      "passes": true,
      "notes": "Delegated to python-hwpx add_picture. Output validated via our gate. Image bytes are read from a client-provided file path.",
      "acceptanceCriteria": [
        "delegate.add_picture(path, image_path, out_path, width_mm, height_mm) reads the image bytes, infers format from the extension, delegates HwpxDocument.add_picture, saves, and validates the output",
        "New MCP tool add_image(path, image_path, out_path, width_mm, height_mm); available:false when python-hwpx is absent",
        "Output passes validate_hwpx (valid True) and the document's image count increases by one",
        "Tests (importorskip hwpx) using a tiny synthetic PNG; pyflakes clean; full suite green"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-028",
      "title": "OWN 갭 마감 — verify_fill / get_table_map / find_cell_by_label",
      "milestone": "M6-PhaseB",
      "priority": 29,
      "passes": true,
      "notes": "Closes the remaining gap-analysis §A items in our core area, exposing existing analyze/understand as MCP tools + a verification helper. Read-only, OWN, no deps.",
      "acceptanceCriteria": [
        "verify_fill(path, expected) confirms each expected value actually appears in the document text; returns verified + present[] + missing[] (whitespace-insensitive)",
        "get_table_map(path) exposes analyze cells structured: per table rows/cols and per cell field_id/row/col/spans/text/is_empty",
        "find_cell_by_label(path, label) returns the label cell field_id(s) and the mapped value-cell field_id (via understand)",
        "Three new read-only MCP tools; built on existing analyze/understand/extract; golden tests on the real fixture; pyflakes clean; full suite green"
      ],
      "status": "complete"
    },
    {
      "id": "US-029",
      "title": "라이브 셀 채우기 — 누름틀 없이 열린 한글 문서의 셀을 직접 채우기",
      "milestone": "M3-live",
      "priority": 30,
      "passes": false,
      "notes": "Headline capability (live fill of the OPEN window) can ONLY be validated in an interactive Windows session with Hangul (i.e. Claude Desktop). Confirmed via probe: this agent runs in non-interactive Session 0 and cannot drive/attach to the user's Session-1 Hangul. Unit-testable parts (target resolution + graceful degradation) pass; live behavior is pending user validation in Claude Desktop.",
      "acceptanceCriteria": [
        "resolve_cell_targets(path, values) maps each value key (field_id or label) to a table-cell address (table/row/col) using analyze/understand — PURE, unit-tested",
        "apply_cells_to_open attaches to the RUNNING Hangul instance via pyhwpx (Running Object Table, on_quit=False so the window is NOT closed), navigates with get_into_nth_table + goto_addr, optionally clears the cell, and inserts the value — for cell-based (empty_cell) forms with no 누름틀",
        "Graceful available:false when pyhwpx/Windows is absent (no crash) — unit-tested",
        "New MCP tool apply_cells_to_open_hwp(path, values); apply_to_open_hwp's needs_field_registration response points cell-forms to it; optional 'live' extra (pyhwpx)",
        "LIVE VALIDATION (client-side, PENDING): filling the open 강사카드 cells succeeds live without field registration — to be confirmed in Claude Desktop"
      ],
      "status": "desktop-live-pending",
      "statusNote": "live cell fill code + pure resolve tests complete; real-desktop verification pending (D7 nested-table mapping is best-effort)."
    },
    {
      "id": "US-030",
      "title": "form-fit 정확도 — 병합셀 폭 + 미세축소 오탐 감소 (실사용 피드백)",
      "milestone": "M7-reliability",
      "priority": 31,
      "passes": true,
      "acceptanceCriteria": [
        "Investigation: cellSz width already reflects colSpan (merged cells) — formfit width is span-aware; documented",
        "overflow_scale gains a deadband (default 1.05): text within ~5% of the cell width is treated as fitting (no 94-99% micro-shrinks)",
        "fill reports overflow warnings ALWAYS (FillResult.overflow), even without auto_fit, so clients can shorten instead of shrink",
        "Golden + regression; existing formfit tests stay green; pyflakes clean"
      ],
      "status": "complete"
    },
    {
      "id": "US-031",
      "title": "체크박스 라벨 유도 개선 + 선택유형 힌트 (실사용 피드백)",
      "milestone": "M7-reliability",
      "priority": 32,
      "passes": true,
      "acceptanceCriteria": [
        "Label derivation: leading text -> left cell -> header cell above -> descriptive fallback from option texts (fixes unidentifiable '선택N')",
        "select_type hint (yes_no|select) exposed via template; option-label cap widened 24->40 for long section titles",
        "Golden + regression; existing checkbox tests stay green; pyflakes clean"
      ],
      "status": "complete"
    },
    {
      "id": "US-032",
      "title": "analyze_form 용량 힌트 (capacity_hint) — 생성 전 셀 크기 인지",
      "milestone": "M7-reliability",
      "priority": 33,
      "passes": true,
      "acceptanceCriteria": [
        "Empty-cell fields carry capacity_hint = approx max Korean chars = (cell width - inner margin) / font height",
        "Computed in understand (lazy formfit import, no cycle), surfaced in analyze_form _field_dict",
        "Golden tests on the real fixture; pyflakes clean"
      ],
      "status": "complete"
    },
    {
      "id": "US-033",
      "title": "PII 자동 경고 (warn-first) in fill",
      "milestone": "M7-reliability",
      "priority": 34,
      "passes": true,
      "acceptanceCriteria": [
        "fill scans every value for PII on every run and reports FillResult.pii_warnings ({key, types, masked}) regardless of mask_pii",
        "mask_pii=True marks masked:true and masks the value; off flags but leaves as-is; surfaced in fill_form",
        "Golden + regression; existing PII tests stay green; pyflakes clean"
      ],
      "status": "complete"
    },
    {
      "id": "US-034",
      "title": "DELEGATE 리치 서식 — emphasize_text (bold/italic/underline/color/size)",
      "milestone": "M8-PhaseC-D",
      "priority": 35,
      "passes": true,
      "notes": "Whole-run scope (does not split sub-run substrings). Structurally verified: a matched run's .bold is True after reopening.",
      "acceptanceCriteria": [
        "emphasize_text(path, find, out, bold/italic/underline/color/size) applies one ensured charPr (ensure_run_style) to every run whose text contains find, saves, and validates",
        "New MCP tool emphasize_text returning matched_runs; available:false when python-hwpx absent",
        "Tests (importorskip): a matched run becomes bold after reopening; color/size output stays valid; no-match is a valid no-op; pyflakes clean"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-035",
      "title": "DELEGATE 표 생성 — 2D 데이터로 채운 표 (create_hwpx_table)",
      "milestone": "M8-PhaseC-D",
      "priority": 36,
      "passes": true,
      "acceptanceCriteria": [
        "create_table_from_rows(rows, out) builds a new HWPX with one table sized to the data (ragged rows padded), fills via set_cell_text, saves, validates",
        "New MCP tool create_hwpx_table(rows, out_path); available:false when python-hwpx absent",
        "Tests (importorskip): content present, correct rows/cols in outline, ragged padded, empty rejected, output valid; pyflakes clean"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-036",
      "title": "DELEGATE 공문 레시피 — create_official_document",
      "milestone": "M9-PhaseD",
      "priority": 37,
      "passes": true,
      "notes": "두뇌·손 분리: 문안은 클라이언트, 서버는 표준 공문 스켈레톤 조립만. 추가 문서유형(보도자료/기안문)은 후속.",
      "acceptanceCriteria": [
        "create_official_document(fields, out) lays out a 공문 skeleton from 기관명/수신/참조/제목/본문/날짜/발신명의/담당자 and validates",
        "New MCP tool create_official_document; unknown keys ignored; available:false when python-hwpx absent",
        "Tests (importorskip): content present in order (수신 < 제목 < 본문), unknown keys ignored, output valid; pyflakes clean"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-037",
      "title": "DELEGATE 레시피 확장 — 보도자료 + 기안문 (doc_type)",
      "milestone": "M9-PhaseD",
      "priority": 38,
      "passes": true,
      "acceptanceCriteria": [
        "create_official_document gains a doc_type param dispatching to per-type skeleton builders: 공문, 보도자료(기관명/배포일/담당/연락처/제목/부제/본문/문의), 기안문(제목/기안자/기안일/시행일/수신/목적/내용/붙임)",
        "Unknown doc_type falls back to 공문; doc_type reported in the result; MCP tool gains doc_type",
        "Tests (importorskip): each type's content present + output valid + fallback; pyflakes clean"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-038",
      "title": "DELEGATE 기존 표 merge + 셀음영",
      "milestone": "M10-Optional-Extensions",
      "priority": 39,
      "passes": true,
      "notes": "python-hwpx 2.24.0 exposes table.merge_cells and table.set_cell_shading; use delegated edit + validate gate.",
      "acceptanceCriteria": [
        "merge_table_cells(path, table_index, cell_range, out_path) merges an existing table range and validates output",
        "set_cell_shading(path, table_index, row, col, fill_color, out_path) applies cell shading and validates output",
        "New MCP tools return available:false without python-hwpx and structured ok:false errors for invalid table/range/color",
        "Tests cover merge, shading, invalid inputs, server tools, and validate_hwpx"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-039",
      "title": "DELEGATE complete-control builder — create_document_from_blocks",
      "milestone": "M10-Optional-Extensions",
      "priority": 40,
      "passes": true,
      "notes": "D6 escape hatch: all content and layout blocks are client-provided; no hidden prose or recipe fallback.",
      "acceptanceCriteria": [
        "create_document_from_blocks(blocks, out_path) supports heading, paragraph, bullet_list, numbered_list, table, image, and page_break blocks",
        "New MCP tool exposes the builder and validates output",
        "Unknown block types, empty blocks, and malformed tables return structured ok:false errors",
        "Tests verify block order, text presence, table output, server tool shape, and validate_hwpx"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-040",
      "title": "구조보존 markdown → HWPX supported subset",
      "milestone": "M10-Optional-Extensions",
      "priority": 41,
      "passes": true,
      "notes": "Route markdown through create_document_from_blocks; support a documented subset, not full CommonMark.",
      "acceptanceCriteria": [
        "create_hwpx_from_markdown preserves headings, unordered/ordered lists, paragraphs, and pipe tables as document structure where supported",
        "Existing simple markdown creation remains valid and text-compatible",
        "README documents the supported subset and non-goals",
        "Tests verify heading/list/table structure, server tool output, and validate_hwpx"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extra 'delegate' (python-hwpx); returns available:false without it; tests importorskip('hwpx')."
    },
    {
      "id": "US-041",
      "title": "render_preview PNG — browser-backed optional preview",
      "milestone": "M10-Optional-Extensions",
      "priority": 42,
      "passes": true,
      "notes": "Use local HTTP serving for Playwright screenshots; no file:// rendering.",
      "acceptanceCriteria": [
        "render_preview(path, out_path, format='png') creates a PNG when Playwright and a browser are available",
        "The tool returns available:false with a setup hint when render dependencies are unavailable",
        "Generated PNGs report file size and dimensions when possible",
        "Tests cover missing dependency behavior and at least one real local PNG proof when the browser is installed"
      ],
      "status": "optional-gated",
      "statusNote": "requires optional extras 'render' (playwright + chromium); returns available:false without them."
    },
    {
      "id": "US-042",
      "title": ".hwp 헤드리스 읽기 adapter gate",
      "milestone": "M10-Optional-Extensions",
      "priority": 43,
      "passes": true,
      "notes": "No local pyhwp/hwp5/rhwp/kordoc substrate is installed yet; do not mislabel COM conversion as headless reading.",
      "acceptanceCriteria": [
        "extract_hwp_text(path) exposes a non-COM headless adapter surface",
        "When no substrate is installed, the tool returns available:false with checked candidate modules and no COM side effects",
        "If a substrate is later installed, a real PII-free .hwp fixture must pass before marking complete",
        "Tests cover dependency-missing behavior and preserve existing COM conversion policy tests"
      ],
      "status": "spike-pending",
      "statusNote": "adapter gate delivered and tested; actual non-COM .hwp extraction awaits substrate decision (ADR D12, US-054/US-055)."
    },
    {
      "id": "US-043",
      "title": "BYO-AI 로컬 하네스 제품 방향 문서화",
      "milestone": "M11-BYO-AI-Live-Harness",
      "priority": 44,
      "passes": true,
      "acceptanceCriteria": [
        "README states that Hangeul-mcp is a local MCP document engine, not a server-side LLM/API billing app",
        "docs/byo-ai-harness.md documents file fill, document generation, live preview/apply, and privacy boundaries",
        "DECISIONS records the no server-side LLM/API boundary and the FILE vs LIVE split"
      ],
      "status": "complete"
    },
    {
      "id": "US-044",
      "title": "Capability manifest for MCP clients",
      "milestone": "M11-BYO-AI-Live-Harness",
      "priority": 45,
      "passes": true,
      "acceptanceCriteria": [
        "describe_capabilities returns product, mode=byo_ai_local_harness, server_side_llm=false, runtime, and capability groups",
        "Capability groups include file_hwpx, delegate_hwpx, render, live_hwp, and hwp_headless",
        "MCP server exposes describe_capabilities and tests verify the tool shape"
      ],
      "status": "complete"
    },
    {
      "id": "US-045",
      "title": "Live cell-fill preview before apply",
      "milestone": "M11-BYO-AI-Live-Harness",
      "priority": 46,
      "passes": true,
      "acceptanceCriteria": [
        "preview_cells_to_open_hwp resolves table/row/col targets without dispatching COM or touching an open Hangul window",
        "apply_cells_to_open_hwp remains the only cell live-apply tool that mutates the open document",
        "Tests cover pure preview target resolution and server tool registration"
      ],
      "status": "complete"
    },
    {
      "id": "US-046",
      "title": "No-API dependency guard",
      "milestone": "M11-BYO-AI-Live-Harness",
      "priority": 47,
      "passes": true,
      "acceptanceCriteria": [
        "Project dependencies do not add OpenAI/Anthropic/Gemini/PydanticAI SDKs",
        "Tests assert no banned LLM API SDK dependency is present",
        "Docs clarify that user's AI client may still decide what document data it sends to its own model"
      ],
      "status": "complete"
    },
    {
      "id": "US-047",
      "title": "prd.json 기계판독 상태 매트릭스 도입",
      "milestone": "M12-Doc-Runtime-Truth",
      "priority": "P0",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "every story has status in statusModel.enum; complete implies passes==true; pending statuses carry statusNote",
        "tests/test_prd_status.py enforces the invariants",
        "existing story contents unchanged (only status/statusNote added)"
      ]
    },
    {
      "id": "US-048",
      "title": "README 카운트 동기화 + 파싱대상 특정 드리프트 가드",
      "milestone": "M12-Doc-Runtime-Truth",
      "priority": "P0",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "README 'NN tools' (repo-tree) dynamically equals len(mcp.list_tools()) via tests/test_maintainability.py",
        "README status line 'NN개 — MM pass' dynamically equals prd story count and passes==true count",
        "both occurrences parsed by labeled regexes; test-count guard stays soft (documented regeneration)"
      ]
    },
    {
      "id": "US-049",
      "title": "HANDOFF.md 대체 + 상태 매트릭스 SSOT",
      "milestone": "M12-Doc-Runtime-Truth",
      "priority": "P0",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "HANDOFF.md rewritten to current baseline with 4-state matrix SSOT",
        "stale tokens ('56 passed', '6툴', 'Phase A~D 전부') grep 0 hits",
        "README/ROADMAP/HANDOFF counts mutually consistent"
      ]
    },
    {
      "id": "US-050",
      "title": "파일모드 e2e 증거팩 드라이버",
      "milestone": "M13-FileMode-Evidence",
      "priority": "P1",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "scripts/e2e_evidence.py runs capabilities->analyze->scan_pii->fill(dry)->fill->verify->validate->render chain on sample_form.hwpx",
        "evidence under gitignored build/evidence/; regeneration documented",
        "dry_run writes nothing; real fill byte-preserves untouched entries; render available:false recorded honestly when absent"
      ]
    },
    {
      "id": "US-051",
      "title": "CI extras 레인 dev+delegate+render",
      "milestone": "M13-FileMode-Evidence",
      "priority": "P1",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "CI job installs .[dev,delegate,render] + playwright chromium and runs pytest on ubuntu",
        "win32-only live/com tests not forced in CI",
        "local-extras baseline vs CI core lane distinction documented"
      ]
    },
    {
      "id": "US-052",
      "title": "라이브 QA 준비 — fixture/runbook + 순수부 테스트",
      "milestone": "M14-Live-Desktop-QA",
      "priority": "P1",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "PII-free live fixture + values mapping + runbook (hwp_status -> preview -> apply)",
        "preview pure computation, hwp_status side-effect-free, apply available:false without substrate — all unit-tested"
      ]
    },
    {
      "id": "US-053",
      "title": "live apply 실기기 검증 또는 명시적 PENDING",
      "milestone": "M14-Live-Desktop-QA",
      "priority": "P1",
      "passes": true,
      "status": "desktop-live-pending",
      "acceptanceCriteria": [
        "desktop evidence captured OR PENDING_DESKTOP_LIVE_QA.md exists with observed hwp_status output",
        "US-029 not promoted without desktop evidence; D7 best-effort re-affirmed"
      ],
      "statusNote": "explicit pending closure: headless session — hwp_status available:true/connected:false observed; live apply evidence not obtainable; PENDING_DESKTOP_LIVE_QA.md is the status truth; US-029 NOT promoted."
    },
    {
      "id": "US-054",
      "title": ".hwp headless substrate 후보 비교 스파이크 + ADR(D12)",
      "milestone": "M15-HWP-Headless-Spike",
      "priority": "P1",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "candidate table (license/installability/API/fixture) + go/no-go recorded as ADR D12",
        "no viable substrate => adapter gate kept; test_hwp_headless green; COM never disguised as headless (D9)"
      ]
    },
    {
      "id": "US-055",
      "title": "(조건부) substrate 어댑터 구현 또는 keep-gate 종결",
      "milestone": "M15-HWP-Headless-Spike",
      "priority": "P2",
      "passes": true,
      "status": "spike-pending",
      "acceptanceCriteria": [
        "IF go + PII-free .hwp fixture: non-COM extraction test (fails without substrate, passes with)",
        "ELSE zero code change; keep-gate rationale in ADR; spike-pending kept"
      ],
      "statusNote": "keep-gate closure per ADR D12: no viable non-COM substrate (pyhwp AGPL/beta, rhwp/kordoc not pip libs, hwp5 namespace squatted); PII-free .hwp fixture prerequisite unmet; zero code change. olefile/PrvText route deferred to a future story."
    },
    {
      "id": "US-056",
      "title": "상류 delegate API-표면 스파이크 (ADR D13 + surface test)",
      "milestone": "M16-Breadth",
      "priority": "P1",
      "passes": true,
      "status": "complete",
      "acceptanceCriteria": [
        "ADR D13: supported={header/footer, page-setup, split_merged_cell}, unsupported={row/col add-remove, TOC}",
        "tests/test_delegate_api_surface.py: existence of supported methods = hard assert; absence of unsupported = soft tripwire only (BC1, never red on upstream additions)",
        "pyproject delegate floor python-hwpx>=2.24,<3; no tools added in this story"
      ]
    },
    {
      "id": "US-057",
      "title": "머리말/꼬리말 delegate 툴 (set_header/set_footer)",
      "milestone": "M16-Breadth",
      "priority": "P1",
      "passes": true,
      "status": "optional-gated",
      "acceptanceCriteria": [
        "MCP tools wrap set_header_text/set_footer_text with validate gate",
        "BC2: saved package re-opened and header/footer XML asserted to contain the text",
        "absent python-hwpx -> available:false; missing method -> structured 'requires python-hwpx>=2.24'"
      ],
      "statusNote": "requires optional extra 'delegate' (python-hwpx>=2.24); available:false without it."
    },
    {
      "id": "US-058",
      "title": "페이지 설정 delegate 툴 (size/margins/columns)",
      "milestone": "M16-Breadth",
      "priority": "P1",
      "passes": true,
      "status": "optional-gated",
      "acceptanceCriteria": [
        "MCP tools wrap set_page_size/set_page_margins/set_columns (page number only if API-verified) with validate gate",
        "BC2: saved package re-opened; section XML properties asserted changed (validate_hwpx alone insufficient)",
        "invalid args -> structured ok:false"
      ],
      "statusNote": "requires optional extra 'delegate' (python-hwpx>=2.24); available:false without it."
    },
    {
      "id": "US-059",
      "title": "병합셀 분할 delegate 툴 (split_merged_cell)",
      "milestone": "M16-Breadth",
      "priority": "P1",
      "passes": true,
      "status": "optional-gated",
      "acceptanceCriteria": [
        "G1: test asserts the input actually contains a merged cell (loud precondition; vacuous pass impossible)",
        "after split, cell map shows unmerged region; validate_hwpx.valid==True",
        "absent/old python-hwpx -> structured fallback"
      ],
      "statusNote": "requires optional extra 'delegate' (python-hwpx>=2.24); split of MERGED cells only (D13)."
    },
    {
      "id": "US-060",
      "title": "표 행/열·TOC spike-pending 재분류 ADR",
      "milestone": "M16-Breadth",
      "priority": "P2",
      "passes": true,
      "status": "spike-pending",
      "acceptanceCriteria": [
        "ADR records python-hwpx non-support (row/col add-remove, TOC) + OWN feasibility verdict with D1/byte-preservation constraint",
        "no acceptance commitments to unsupported behaviors; zero delegate code for these"
      ],
      "statusNote": "features stay unprovided: python-hwpx 2.24 lacks row/col & TOC APIs (D13); OWN feasibility verdict in ADR D14 (row/col implement-later conditional, page-numbered TOC keep-out); surface tripwire re-triggers review."
    }
  ],
  "decisions": {
    "substrate": "own byte-preserving engine (core) + python-hwpx for XSD validation & security hardening",
    "matching_key": "field_id (authoritative) + label (alias)",
    "hwp_input": "v1 auto-convert to HWPX (COM-first, LibreOffice/H2Orestart fallback)",
    "package_name": "hangeul-mcp",
    "pypi": "deferred to v1.0",
    "git_remote": "pblsketch/Hangeul-mcp (private)"
  },
  "statusModel": {
    "enum": [
      "complete",
      "optional-gated",
      "desktop-live-pending",
      "spike-pending",
      "planned"
    ],
    "rules": [
      "status==complete requires passes==true",
      "passes==false forbids status==complete",
      "desktop-live-pending / spike-pending require statusNote explaining the missing evidence",
      "planned = accepted stabilization-pass story not yet executed (US-047..US-060)"
    ],
    "passCountDefinition": "pass count in docs (README status line) == number of stories with passes==true (BC3/Minor-1; independent of status field)",
    "countsAreMovingOutputs": "story/pass counts move as stories land; README drift guard (tests/test_maintainability.py) compares dynamically (BC3)"
  }
}
